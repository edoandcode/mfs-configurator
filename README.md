# modular-shelving-configurator
Configurator of a modular furnishing system written in Java.

## Getting Started


First, build the project:

```bash
gradle build
```

Second, run the project:

```bash
gradle run
```